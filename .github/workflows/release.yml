name: Release Messenger Desktop

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: '--target x86_64-pc-windows-msvc'
            target: 'x86_64-pc-windows-msvc'
            asset_os: 'Windows'
          - platform: 'ubuntu-22.04'
            args: '--target x86_64-unknown-linux-gnu'
            target: 'x86_64-unknown-linux-gnu'
            asset_os: 'Linux'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🏷️ Set Release Version & Tag
        id: vars
        shell: bash
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            TAG="${{ github.ref_name }}"
          elif [ -n "${{ inputs.version }}" ]; then
            TAG="${{ inputs.version }}"
          else
            TAG="v1.0.0"
          fi
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Using TAG: ${TAG}, VERSION: ${VERSION}"

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 🦀 Setup Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: ⚡ Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: 🐧 Install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            squashfs-tools \
            rpm

      - name: 🏷️ Sync Project Version with Release Tag
        shell: bash
        run: |
          VERSION="${{ steps.vars.outputs.version }}"
          echo "Synchronizing project files to release version: $VERSION"

          # Update package.json
          npm version "$VERSION" --no-git-tag-version --allow-same-version || true

          # Update tauri.conf.json
          node -e "
            const fs = require('fs');
            const file = './src-tauri/tauri.conf.json';
            const json = JSON.parse(fs.readFileSync(file, 'utf8'));
            json.version = '$VERSION';
            fs.writeFileSync(file, JSON.stringify(json, null, 2));
          "

          # Update Cargo.toml
          sed -i.bak -E "0,/^version = .*/s//version = \"$VERSION\"/" ./src-tauri/Cargo.toml && rm -f ./src-tauri/Cargo.toml.bak

      - name: 📦 Install Node dependencies
        run: npm ci || npm install

      - name: 🔨 Build Frontend
        run: npm run build

      - name: 🚀 Build Tauri Application
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ steps.vars.outputs.tag }}
          releaseName: 'Messenger Desktop ${{ steps.vars.outputs.tag }} 🚀'
          releaseBody: |
            # 🚀 Messenger Desktop Release `${{ steps.vars.outputs.tag }}`

            Welcome to the new release of **Messenger Desktop**! A fast, lightweight native desktop wrapper for Facebook Messenger powered by **Tauri v2**, **SolidJS**, and **Rust**.

            ---

            ### ✨ Highlights & Features
            - 🔔 **Native OS Notifications**: Real-time toast alerts with banner actions and sound when friends message you.
            - 🔴 **Unread Badge Counter**: Live badge tracking in the System Tray and Titlebar.
            - 🪟 **System Tray Integration**: Minimize or close to background system tray seamlessly.
            - ⚡ **Ultra-Fast & Memory Efficient**: Uses your operating system's native Webview2 / WebKit engine with minimal RAM footprint.
            - 🔗 **Smart Link Routing**: External links from chats automatically open in your default browser.
            - 🚀 **Autostart Support**: Option to launch automatically on system boot.

            ---

            ### 📦 Download & Installation

            #### 🪟 Windows (10 / 11)
            - **`Messenger_*_x64-setup.exe`**: Standard NSIS Setup Installer.
            - **`Messenger_*_x64_en-US.msi`**: Windows Enterprise / MSI Package.

            #### 🐧 Linux (Ubuntu, Debian, Fedora, Arch Linux)
            - **`Messenger_*_amd64.AppImage`**: Portable Linux AppImage (Works on all distributions).
            - **`messenger-desktop_*_amd64.deb`**: Debian / Ubuntu package (`sudo dpkg -i messenger-desktop_*.deb`).
            - **`messenger-desktop-*-1-x86_64.pkg.tar.zst`**: Native Arch Linux package.
            - **Arch Linux One-Command Installer**: `curl -fsSL https://raw.githubusercontent.com/rootLocalGhost/Messenger-Next/main/arch/install-arch.sh | bash`

            ---

            ### 🛠️ Verification & Checksums
            All binaries are built and signed via automated GitHub Actions CI/CD.
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}

      - name: 🏹 Build & Upload Arch Linux .pkg.tar.zst Package
        if: matrix.platform == 'ubuntu-22.04'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.vars.outputs.version }}"
          PKG_NAME="messenger-desktop"
          ARCH_PKG_NAME="${PKG_NAME}-${VERSION}-1-x86_64.pkg.tar.zst"

          echo "Packaging ${ARCH_PKG_NAME}..."
          PKG_DIR=$(mktemp -d)
          mkdir -p "${PKG_DIR}/usr/bin"
          mkdir -p "${PKG_DIR}/usr/share/applications"
          mkdir -p "${PKG_DIR}/usr/share/icons/hicolor/32x32/apps"
          mkdir -p "${PKG_DIR}/usr/share/icons/hicolor/128x128/apps"
          mkdir -p "${PKG_DIR}/usr/share/icons/hicolor/256x256/apps"
          mkdir -p "${PKG_DIR}/usr/share/icons/hicolor/512x512/apps"

          cp "src-tauri/target/${{ matrix.target }}/release/${PKG_NAME}" "${PKG_DIR}/usr/bin/${PKG_NAME}" 2>/dev/null || cp "src-tauri/target/release/${PKG_NAME}" "${PKG_DIR}/usr/bin/${PKG_NAME}"
          cp "arch/${PKG_NAME}.desktop" "${PKG_DIR}/usr/share/applications/${PKG_NAME}.desktop"
          cp "src-tauri/icons/32x32.png" "${PKG_DIR}/usr/share/icons/hicolor/32x32/apps/${PKG_NAME}.png"
          cp "src-tauri/icons/128x128.png" "${PKG_DIR}/usr/share/icons/hicolor/128x128/apps/${PKG_NAME}.png"
          cp "src-tauri/icons/128x128@2x.png" "${PKG_DIR}/usr/share/icons/hicolor/256x256/apps/${PKG_NAME}.png"
          cp "src-tauri/icons/icon.png" "${PKG_DIR}/usr/share/icons/hicolor/512x512/apps/${PKG_NAME}.png"

          INSTALLED_SIZE=$(du -sb "${PKG_DIR}/usr" | awk '{print $1}')
          BUILDDATE=$(date +%s)

          {
            echo "pkgname = ${PKG_NAME}"
            echo "pkgbase = ${PKG_NAME}"
            echo "pkgver = ${VERSION}-1"
            echo "pkgdesc = Facebook Messenger desktop application with native notifications built with Tauri v2"
            echo "url = https://github.com/rootLocalGhost/Messenger-Next"
            echo "builddate = ${BUILDDATE}"
            echo "packager = rootLocalGhost <rootlocalghost@gmail.com>"
            echo "size = ${INSTALLED_SIZE}"
            echo "arch = x86_64"
            echo "license = MIT"
            echo "depend = webkit2gtk-4.1"
            echo "depend = gtk3"
            echo "depend = libayatana-appindicator"
            echo "depend = openssl"
            echo "depend = cairo"
            echo "depend = gdk-pixbuf2"
            echo "depend = glib2"
            echo "depend = pango"
            echo "depend = hicolor-icon-theme"
            echo "provides = ${PKG_NAME}"
            echo "conflict = ${PKG_NAME}-bin"
          } > "${PKG_DIR}/.PKGINFO"

          (cd "${PKG_DIR}" && tar -c --zstd -f "${GITHUB_WORKSPACE}/${ARCH_PKG_NAME}" .PKGINFO usr)
          gh release upload "${{ steps.vars.outputs.tag }}" "${GITHUB_WORKSPACE}/${ARCH_PKG_NAME}" --clobber
